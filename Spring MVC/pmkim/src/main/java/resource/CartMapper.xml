<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="resource.CartMapper">
	<select id="selectCart" resultType="vo.CartVO">
		select cartId,id,goodId 
		from cart 
		where id = #{id}
	</select>
	
	<delete id="deleteCart" parameterType="String">
		delete from cart where id=#{id}
	</delete>

	<insert id="insertCart" parameterType="vo.CartVO">
		<selectKey resultType="_int" keyProperty="cartId" order="BEFORE">
			select cart_seq.nextval from dual
		</selectKey>
		insert into cart (cartId, id, goodId) values (#{cartId},#{id},#{goodId})
	</insert>
	
	<select id="searchGoods" resultType="vo.GoodsVO">
		select goodId, goodName, goodPrice, mfgName, goodImg 
		from goods 
		where goodName like '%'||#{keyword}||'%'
	</select>
</mapper>